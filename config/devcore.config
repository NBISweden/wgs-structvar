process {

   clusterOptions = {
       "-A $params.project"
   }
   errorStrategy = { task.exitStatus == 143 ? 'retry' : 'terminate' }
   queue = 'devcore'
   executor = 'slurm'

   time = '1h'

}

executor {
    $slurm {
        queueSize = 1
    }
    $local {
        queueSize = 1
    }
}
